# 安全设计

路遥智控会操作当前登录用户的 Windows 桌面。使用前请理解以下边界：

- MCP 接入点地址通常包含 Token，应视为密码，不要截图或公开。
- 勾选“保存地址”后，地址使用当前 Windows 用户的 DPAPI 加密保存。
- 应用启动受白名单限制；未启用的别名不能被 MCP 调用。
- 网页工具只接受 `http://` 与 `https://`，不能直接打开本地文件。
- 关闭应用只请求窗口正常退出，不会强制结束进程。
- 不提供任意 PowerShell、CMD、文件删除、软件安装或远程管理员提权工具。
- 程序默认以普通用户权限运行。只有用户在本机点击“管理员模式重启”并亲自确认 Windows UAC 后才会提升；UAC 安全桌面始终不能自动控制。
- 鼠标、键盘、屏幕文字识别、剪贴板、系统设置/电源操作与截图权限默认关闭。
- 屏幕文字识别只在内存中截取当前前台窗口，截图不落盘、不上传；识别出的可见文字会返回给当前 MCP 连接。编号点击只接受 90 秒内的最近快照，并在点击前重新核对窗口、位置和文字。
- “按屏幕文字打开”只会对当前画面中实际识别到的文字执行鼠标单击或双击，不接受可执行路径、网址或命令；多个同名结果必须明确指定位置。
- 可见搜索流程必须先输入并在原搜索框位置核对文字，再根据画面使用唯一搜索按钮或回车；核对失败时不会提交。
- 搜索框位置记忆只保存在当前运行会话中，断开连接或紧急停止后清除。导出的诊断报告会包含最近最多 80 条工具名称、结果、耗时和前台进程，可能包含搜索词或联系人名称，但不会记录聊天消息正文；报告只在用户主动导出时写入本机。
- QQ 和微信消息分为准备与确认两步。准备阶段每次重新搜索唯一联系人、核对会话标题并只填入草稿；后续明确确认时再次核对联系人和草稿。一次性编号两分钟过期、使用前即消费，重复确认不会重复发送。
- 读取剪贴板只返回纯文字且最多 4000 个字符；剪贴板可能含有密码或隐私，只有用户单独开启权限后才会注册该工具。
- 系统设置工具只接受固定枚举动作和受限参数，不接受脚本或命令。壁纸必须是本机已存在的 BMP/JPG/JPEG/PNG 文件，且最大 50 MB。
- 锁定、关机和重启分为准备与确认两步。准备不会修改系统；一次性编号两分钟过期并在操作系统调用前消费，网络重试不会重复执行。取消已计划的关机或重启不需要二次确认。
- PPT/WPS 控制会先核对前台进程，只对已识别的演示程序发送快捷键。
- “紧急停止”会关闭控制权限并断开 MCP 连接。
- 正式 Release 构建会对应用程序集执行名称混淆、字符串隐藏和 IL 优化，且不发布源码、PDB 或混淆映射；便携包只保留程序和使用说明。该措施只能提高逆向与篡改成本，不能承诺任何本机软件“绝对无法反编译”。
- 代码签名需要创作人持有受信任的 Windows 代码签名证书。构建流程已为后续签名留出位置；在没有证书时不会伪造或使用自签名证书冒充可信发布者。

如果需要新增系统命令，建议把每个动作实现为独立、参数受限的工具，不要增加无限制 Shell 工具。
